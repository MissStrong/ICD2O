/* 
 * This is just a suggested template. 
 * Feel free to completely change it if you want.
 */

const numObjects = 10; // change the name and number
let objects = []; // change the name

function setup() {
  createCanvas(windowWidth, windowHeight);
  for (let index = 0; index < numObjects; index++) { // rename numObjects
    let object = {  // change the name
      property1: 0, // change the name and number
      property2: 0, // change the name and number
      property3: 0 // change the name and number
      // add more properties here
      }
    }
    object.push(circle); // puts the object into the array
  }
}

function draw() {
  for (let object of objects) { // rename these
    // Display the object on the screen here
    // Update their properties as needed
  }
}
